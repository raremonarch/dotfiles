# Separate Claude Code instance for enterprise/work use — isolated config,
# credentials, memory, and session history via CLAUDE_CONFIG_DIR.
alias claude-work='CLAUDE_CONFIG_DIR=~/.claude-work claude'
